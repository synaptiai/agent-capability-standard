# Changelog

## v1.0.0 — 2026-01-24
- Initial publication candidate.
- Capability ontology with contracts + dependencies.
- Workflow DSL spec v2 with bindings, conditions, gates, recovery loops, parallel groups.
- Canonical world state + event schemas (provenance, uncertainty, retention, lineage).
- Identity taxonomy + identity resolution policy.
- Authority trust model (weights + decay + field-specific authority).
- Compiler-grade validator with:
  - $ref resolution
  - type inference
  - consumer input schema checking
  - patch suggestions and optional diff output
